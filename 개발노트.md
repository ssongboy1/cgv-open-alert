# 개발노트

다음에 이 프로젝트를 손댈 때 먼저 읽을 문서. 사용법은 [README.md](README.md).

---

## 1. 한눈에 보는 구조

```
cron-job.org  ──5분마다 POST──▶  GitHub API /dispatches
                                        │
                                        ▼
                              GitHub Actions (watch.yml)
                                        │
                       timeout 240 python watcher.py --loop 60
                                        │
                       ┌────────────────┼────────────────┐
                       ▼                ▼                ▼
                  CGV BFF 조회    텔레그램 명령 처리    알림 전송
                                  (long polling)
                                        │
                       변화 있을 때만 state.json / config.json 커밋
```

**왜 5분마다 깨워서 4분을 돌리나** — 러너 부팅에 30~40초가 든다. 1분마다 새로
띄우면 하루 1,440번 부팅하면서 오버헤드가 실제 일보다 10배 커진다. 한 번
깨워 4번 쓰면 부팅 비용이 분산되고, GitHub 부담도 1/5이 된다.

**왜 GitHub 자체 cron 을 안 쓰나** — 실측 **+18분 지연**을 봤다(2026-08-20
11:00 예정 → 11:18 실행). 예매 오픈에 18분이면 끝난 거다. cron-job.org 는
오차 ±10초였다. GitHub cron 은 1시간짜리 안전망으로만 남겼다.

---

## 2. CGV API — 알아낸 것 전부

### 기본

신규 사이트(Next.js)는 `api.cgv.co.kr` 을 직접 부르지 않고 **BFF** 를 거친다.
매핑 규칙은 번들 `_next/static/chunks/2036-*.js` 에 있다.

```
/cnm/atkt/*  ->  https://cgv.co.kr/api/v1/booking/*
/cnm/*       ->  https://cgv.co.kr/api/v1/content/*
```

**토큰·쿠키 불필요.** `coCd=A420`(CGV 회사코드)만 있으면 되고 전부 `GET`.

### 쓰는 엔드포인트

| 용도 | `https://cgv.co.kr/api/v1/booking/...` | 필수 파라미터 |
| --- | --- | --- |
| 지역·지점 목록 | `searchRegnList` | `coCd` |
| 지점 특별관 현황 | `searchSscnsSchdExistList` | `coCd`, `siteNo` |
| 예매 가능 날짜 | `searchSiteScnscYmdListBySite` | `coCd`, `siteNo` |
| **시간표(핵심)** | `searchMovScnInfo` | `coCd`, `rtctlScopCd=01`, `siteNo`, `scnYmd` |
| 영화 목록 | `searchAtktTopPostrList` | `coCd` |

### 응답 필드

`searchMovScnInfo` 에서 쓰는 것:

| 필드 | 의미 | 예 |
| --- | --- | --- |
| `tcscnsGradNm` | 특별관 등급 — **필터 기준** | `아이맥스` `4DX` `SCREENX` `일반` |
| `movkndDsplNm` | 앱에 뜨는 규격 — **알림에 표시** | `IMAX LASER 2D` |
| `scnsNm` | 상영관 이름 | `IMAX관` `1관 (Laser)` |
| `scnsrtTm` | 시작 시각 | `0800`, 심야는 `2440` |
| `frSeatCnt` / `stcnt` | 잔여 / 총좌석 | `9` / `241` |
| `movNo` / `movNm` | 영화 번호 / 제목 | |
| `scnsNo` | 상영관 번호 | `018` |

### 함정들

**`schdCnt` 는 회차 수가 아니라 영화 편수다.** `searchSscnsSchdExistList` 의
`schdCnt` 를 회차 수로 오해하기 쉽다. 용산 아이맥스가 `1` 인데 실제로는
하루 6회차였다. 오디세이 1편이라 1이다.

**심야 회차는 24시를 넘겨서 준다.** `2440` = 다음날 00:40. 앱도 `24:40` 으로
표기하므로 그대로 보여주는 게 맞다. 24시간제로 변환하면 앱과 달라진다.

**앱은 매진 회차를 숨긴다.** API 는 `frSeatCnt=0` 으로 그대로 준다.
앱 화면과 회차 수가 안 맞으면 이것부터 의심할 것. (스크린샷 대조로 확인:
10회차 중 9개 잔여석 정확히 일치, 1개는 매진이라 앱에서만 없었다)

**`searchAtktTopPostrList` 에는 개봉예정작도 들어 있다.** 예매가 아직 안
열린 영화는 `atktRate` 가 비어 있다(실측: 65편 중 14편, 전부 예매 가능
날짜 0일). 덕분에 **조회 한 번으로 미개봉작까지 제목 검증**이 된다.

**`siteNo` 를 넘겨도 무시하는 엔드포인트가 있다.** `searchAtktTopPostrList`
는 `siteNo` 를 줘도 전국 목록을 준다. 지점별 상영작 목록 전용 엔드포인트는
찾지 못했다.

### 못 찾은 것

- 지점별 영화 목록 (그래서 영화 단위 감시는 전체 스캔에 의존)
- `cgv://` 뒤에 붙일 경로 (앱 첫 화면까지만 열림)
- 개봉예정작 전용 엔드포인트 (위 목록으로 대체됨)

---

## 3. Cloudflare — 절대 건드리면 안 되는 것

`cgv.co.kr` 앞단은 Cloudflare 다. **차단 기준은 User-Agent 문자열 하나뿐이다.**

| UA | 결과 |
| --- | --- |
| `curl/8.4.0` | **403** |
| `python-requests/2.32.0` | **403** |
| `Mozilla/5.0 ... Chrome/131` | 200 |
| `Mozilla/5.0 (Windows NT 6.1) Chrome/60.0` (구형) | 200 |

Referer·Accept·쿠키·JS 챌린지 전부 불필요. IP 기반도 아니다 — GitHub Actions
데이터센터 IP 에서도 200 이 나온다(smoke-test.yml 로 확인).

**`cgv_api.UA` 상수를 지우거나 기본값으로 되돌리면 즉시 전부 403 이 된다.**
`selftest.py` 가 이걸 회귀 테스트로 잡고 있다.

속도 제한은 없었다. 지연 없이 30회 연속 던져 **12.6 req/s 로 전부 200**.
우리는 33초에 1개(0.03 req/s)를 보낸다. 400배 여유.

---

## 4. 감지 로직

### 2단계 게이트

매번 전체 시간표를 훑으면 지점당 13요청이다. 그래서 먼저 **2요청짜리
게이트**만 본다.

| 게이트 | 보는 값 |
| --- | --- |
| `searchSscnsSchdExistList` | 그 상영관에서 상영 중인 **영화 편수** |
| `searchSiteScnscYmdListBySite` | **예매 가능한 날짜 목록** |

둘 다 지난번과 같으면 상세 조회를 건너뛴다.

| 실제 상황 | 게이트가 잡나 |
| --- | --- |
| 새 주차 개방 (날짜 추가) | **즉시** — 날짜 목록 변화 |
| 새 영화 등록 | **즉시** — 편수 변화 |
| 증회 (기존 날짜·영화에 회차만 추가) | **못 잡음** → 30분 정기 스캔이 잡음 |

증회를 빨리 보려면 `config.json` 의 `full_scan_every_runs` 를 30에서 10으로
낮춘다. CGV 요청이 하루 2,600 → 4,000 으로 는다.

### 회차 식별 키

```
site_no | movNo | scnYmd | scnsrtTm | scnsNo     (회차 단위)
M | site_no | movNo                              (영화 단위)
```

`state.json` 의 `seen` 에 없으면 신규다. `prune_seen()` 이 키를 정리하는데
**회차 키는 3번째 조각이 상영일자**라는 전제로 동작한다. 키 형식을 바꾸면
거기도 같이 고쳐야 한다.

### 기준선(baseline)

`state.baselined` 에 **대상별로** 기록한다. 새 대상을 추가하면 그 시점에
이미 열려 있던 회차는 조용히 기준선으로만 저장하고 알리지 않는다.

`target_id()` = `site_no|screen|정규화한제목|알림단위`. 대상을 지우면 그
기록도 지운다(재등록 시 다시 기준선부터).

---

## 5. 텔레그램 — 제약과 우회

**서버 없이 봇 명령을 받는 법.** 웹훅을 쓸 수 없어서, 감시 사이클 사이의
쉬는 시간에 `getUpdates` **long polling** 을 건다. 어차피 자던 시간이라
비용이 0이고, 명령 반응이 1분 → 1초로 줄었다.

**링크 제약이 심하다.** 실측 결과:

| 방식 | 결과 |
| --- | --- |
| 본문에 `cgv://` | API 는 받지만 **링크로 렌더링 안 됨** (그냥 검은 글씨) |
| 본문에 `intent://` | 마찬가지로 렌더링 안 됨 |
| 인라인 버튼에 `cgv://` | API 가 400 거부 (`Unsupported URL protocol`) |
| 인라인 버튼에 `https://` | 정상 |

그래서 **https 중계 페이지**(`docs/open.html`, GitHub Pages)를 거친다.
버튼 → 중계 페이지 → `cgv://` → 앱.

**CGV 앱 정보 (실기기 확인)**

- 패키지: `co.kr.cgv.cjcgv`
- 스킴: `cgv://` — 동작함
- `intent://#Intent;scheme=cgv;package=co.kr.cgv.cjcgv;end` — 동작함
- `cgv.co.kr` 은 App Links 미설정(`/.well-known/assetlinks.json` 없음).
  그래서 https 링크로는 앱이 안 열린다

---

## 6. 그동안 잡은 버그 — 같은 실수 반복 금지

전부 `selftest.py` 에 회귀 테스트로 박혀 있다. **코드 고치면 반드시 돌릴 것.**

**① `prune_seen` 인덱스 오류**
날짜 자리(`parts[2]`) 대신 시각 자리(`parts[3]`)를 오늘 날짜와 비교했다.
`"0700" < "20260820"` 이 참이라 주간 회차 42개가 매번 삭제됐다.
→ 30분마다 같은 알림이 반복될 뻔했다.

**② `state.json` 머지 충돌**
실행이 겹치면 두 러너가 각각 고친 뒤 동시에 push 해서 rebase 가 실패했다.
→ `merge_state.py` 로 `seen` 을 **합집합** 병합. 한쪽을 버리면 알림 기록이
유실돼 중복 알림이 난다.

**③ 새 대상이 기존 회차를 전부 알림**
"첫 실행이면 기준선만" 로직이 **시스템 전체의 첫 실행**에만 걸려 있었다.
돌고 있는 중에 대상을 추가하면 `initialized` 가 이미 True 라 건너뛰었다.
→ 대상별 `baselined` 도입.

**④ 매칭 0건 대상이 기준선에 기록 안 됨** ← 가장 위험했다
`baselined.add()` 가 `if not rows: continue` **뒤에** 있었다. 아직 안 열린
영화를 감시하면 0건이라 기록을 건너뛰고, **정작 예매가 열리는 순간에도
'새 대상'이라 알림 대신 기준선만 잡고 끝났다.** 알리미의 존재 이유가
무너지는 경로.

**⑤ 같은 지점 두 번째 대상이 게이트 변화를 못 봄**
루프 도중 `gates[site]` 를 덮어써서, 뒤 대상은 방금 쓴 값과 비교하게 됐다.
→ 지난 값을 `old_gates` 로 고정. 게이트는 지점당 1회만 조회(요청도 줄었다).

**⑥ 붙임표 매칭 실패**
CGV 제목은 `스파이더맨-브랜드 뉴 데이` 인데 사용자는 `스파이더맨 브랜드 뉴
데이` 로 적는다. `normalize` 가 공백만 지워서 안 맞았다.
→ 문장부호까지 제거.

**⑦ 대상 전체 삭제 시 기준선 잔존**
조기 반환이 정리 코드보다 앞에 있었다. 재등록하면 `seen` 은 비었는데
기준선은 남아 ③번 버그가 되살아난다.

**⑧ SIGTERM 중 파일 손상 가능성**
워크플로가 4분 뒤 강제 종료된다. `save_state()` 는 임시 파일에 쓴 뒤
`replace()` 로 교체한다. 직접 덮어쓰면 반쯤 쓰인 채로 남을 수 있다.

---

## 7. 손대기 전에 알아둘 것

**`cgv_api.UA`** — 지우면 전부 403. 3장 참고.

**회차 키 형식** — `prune_seen()` 이 조각 위치에 의존한다.

**`normalize()`** — 바꾸면 `target_id()` 도 바뀌어서 기존 `baselined` 가
무효가 된다. 한 사이클 동안 전 대상이 '새 대상'으로 보여 조용히 재기준선을
잡는다(알림 폭탄은 안 나지만, 그 순간 열린 회차는 놓친다).

**`state.json` 스키마 추가 시** — `merge_state.py` 에도 병합 규칙을 넣을 것.
안 넣으면 실행이 겹칠 때 한쪽이 통째로 사라진다.

**워크플로 커밋 스텝** — `git pull --rebase` 로 되돌리지 말 것. ②번 버그다.

---

## 8. 자주 쓰는 명령

```bash
python selftest.py --live                 # 60건 전체 점검. 고치면 항상 이것부터

gh run list --workflow=watch.yml --limit 10
gh run view <ID> --log | grep -E "회차|변화|기준선"
gh workflow run smoke-test.yml            # Actions 에서 CGV 403 여부 확인
gh api -X POST repos/ssongboy1/cgv-open-alert/dispatches -f event_type=poll

git fetch origin main && git show origin/main:state.json | python -m json.tool
```

**로컬에서 급하게 돌릴 때** (오픈 임박일 등)

```bash
python watcher.py --loop 30
```

Actions 와 같은 `state.json` 을 쓰지 않으므로 중복 알림이 날 수 있다.
로컬로 돌릴 거면 cron-job.org 를 잠시 꺼두는 게 낫다.

---

## 9. 남은 일 / 알려진 한계

- **실제 예매 오픈을 아직 안 겪었다.** 지금까지는 전부 기준선·모의 테스트다.
  다음 주차 개방 때가 첫 실전. 그때 알림이 오면 완전 검증.
- 증회는 최대 30분 지연 (4장)
- `cgv://` 로 특정 지점까지 딥링크하는 경로를 못 찾았다 (앱 첫 화면만)
- 외부 의존 3곳: CGV API 스펙, Cloudflare 정책, cron-job.org.
  전부 조용히 죽지는 않게 해뒀다(403 → 텔레그램 경고, cron 장애 → 시간별
  안전망, cron-job.org 실패 알림 메일)
- 봇 토큰이 개발 중 대화에 노출됐다. 신경 쓰이면 BotFather `/revoke` 후
  `gh secret set TELEGRAM_BOT_TOKEN` 으로 교체
